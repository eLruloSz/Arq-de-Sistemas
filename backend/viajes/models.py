from django.db import models

# App: viajes
# Responsable de las instancias concretas de un viaje:
# una Ruta + un Bus asignado + una fecha/hora de salida.
# Próximo paso: Viaje, y la consulta de disponibilidad por tramo.
