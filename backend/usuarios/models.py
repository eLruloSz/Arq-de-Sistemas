from django.db import models

# App: usuarios
# Responsable de la autenticación y los roles del sistema
# (pasajero, administrador). Próximo paso: definir el modelo
# de usuario (extendiendo AbstractUser o AbstractBaseUser).
